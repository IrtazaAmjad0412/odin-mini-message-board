<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/styles.css" />
  </head>
  <body>
    <h1 class="heading-one"><%= title %></h1>
    <ul class="list">
      <% messages.forEach((message) => { %>
      <li class="list-item">
        <h2><%= message.user %></h2>
        <p><%= message.text %></p>
        <p><%= message.added.toDateString() %></p>
        <a href="/message/<%= message.id %>">Open</a>
        <form method="POST" action="/message/<%= message.id %>/delete">
          <button type="submit">Delete</button>
        </form>
      </li>
      <% })%>
    </ul>
    <a href="/new" class="link">Add a new message</a>
  </body>
</html>
